# Camera configuration - Raspberry Pi Camera (libcamera via camera_ros)
camera_node:
  ros__parameters:
    # Camera index (0 = primary Pi Camera)
    camera: 0
    # Resolution
    width: 640
    height: 480
    # Framerate
    fps: 58.92
    # TF frame
    frame_id: "camera_link"
